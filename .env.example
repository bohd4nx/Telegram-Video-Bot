# Required. Get from @BotFather.
BOT_TOKEN=

# Required. Matches the values in docker-compose.yml → postgres service.
POSTGRES_USER=videobot
POSTGRES_PASSWORD=videobot
POSTGRES_DB=videobot

# Optional. Defaults to "db" (Docker service name) / "5432".
# POSTGRES_HOST=db
# POSTGRES_PORT=5432
